import math
import sys

we=eval('2+90*90')# eval function is being used to interpretate the given string
                  #as standard input come in the form of string only create it
print(we)

we=eval("kuldeep");print(we)